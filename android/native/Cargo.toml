[package]
name = "fishpi-mobile-native"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "fishpi_sdk"
crate-type = ["cdylib"]

[dependencies]
fishpi-sdk = { path = "../.." }
jni = "0.21.1"
serde_json = "1.0.147"
tokio = { version = "1.48.0", features = ["full"] }
